---

# use heat or ansible os_server?
- name: spawn a bastion host and child VMs

- name: create ansible users on bastion host
  # inventory & group_vars
  roles:
    - ansible-role-users

- name: create ssh keys & ansible users on child VMs

...
